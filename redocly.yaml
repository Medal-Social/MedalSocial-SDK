apis:
  medal-social:
    root: openapi/medal-social.openapi.yaml

extends:
  - recommended

rules:
  operation-operationId: error
  operation-4xx-response: off
  no-unused-components: warn
  security-defined: error
