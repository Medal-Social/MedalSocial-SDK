---
"@medalsocial/sdk": minor
---

Add the scan resource: queue and poll Nettsjekk company/site scans (`medal.scan.create` / `get` / `waitForResult`) and search the Norwegian company registry (`medal.scan.companies`).
